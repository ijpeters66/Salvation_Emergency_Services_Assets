import { test } from "@playwright/test";

test.skip("creating a consumable batch with mocked Supabase", async () => {
  // Requires a server-action aware Supabase test double or a disposable test database.
});

test.skip("issuing stock updates quantity and history with mocked Supabase", async () => {
  // Requires a server-action aware Supabase test double or a disposable test database.
});

test.skip("FIFO issue flow with mocked Supabase", async () => {
  // Requires a server-action aware Supabase test double or a disposable test database.
});
