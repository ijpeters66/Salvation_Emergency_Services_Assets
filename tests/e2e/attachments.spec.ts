import { test } from "@playwright/test";

test.skip("upload flow with mocked Supabase storage", async () => {
  // Requires storage mocking or a disposable Supabase project with authenticated uploads.
});
