import { test } from "@playwright/test";

test.skip("mocked scan routes to the correct asset detail page", async () => {
  // Requires camera or QR-resolution mocking around the protected scan route.
});
